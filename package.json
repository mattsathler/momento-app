{
  "name": "momento-app",
  "version": "3.9.5",
  "main": "generateToken.js",
  "private": true,
  "workspaces": [
    "src/bots/*"
  ],
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "axios": "^1.10.0",
    "discord.js": "^14.22.1",
    "dotenv": "^16.5.0",
    "emoji-regex": "^10.4.0",
    "ffmpeg-extract-frames": "^2.0.2",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.2",
    "gif-encoder-2": "^1.0.3",
    "json": "^11.0.0",
    "mongoose": "^8.15.1",
    "node-cron": "^4.1.0",
    "nodemon": "^3.1.10",
    "pm2": "^6.0.8",
    "qrcode-pix": "^5.0.0",
    "rxjs": "^7.8.2",
    "skia-canvas": "^2.0.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "pm2": "pm2 start ecosystem.config.js",
    "token": "node generateToken.ts",
    "start:core": "npm run start --workspace momento-core",
    "start:notifications": "npm run start --workspace momento-notifications",
    "start:profile-updater": "npm run start --workspace momento-profile-updater",
    "start:analytics": "npm run start --workspace momento-analytics",
    "start:hub": "npm run start --workspace momento-hub",
    "deploy:prod": "ssh -i momento.pem ubuntu@ec2-54-207-197-131.sa-east-1.compute.amazonaws.com \"cd ~/momento-app && git checkout master && git pull origin master && npm install && pm2 kill && npm run pm2\"",
    "update:major": "tsx scripts/updateVersion.ts major && git add package.json && git commit -m \"🏷️ - %npm_package_version%\" && git push origin master && npm run deploy:prod && tsx scripts/publishVersion.ts",
    "update:minor": "tsx scripts/updateVersion.ts minor && git add package.json && git commit -m \"🏷️ - %npm_package_version%\" && git push origin master && npm run deploy:prod && tsx scripts/publishVersion.ts",
    "update:patch": "tsx scripts/updateVersion.ts patch && git add package.json && git commit -m \"🏷️ - %npm_package_version%\" && git push origin master && npm run deploy:prod && tsx scripts/publishVersion.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "commonjs"
}
